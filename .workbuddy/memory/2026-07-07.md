# 2026-07-07

## mall-gateway 模块学习
- 完整梳理了 mall-gateway 网关模块的代码结构、路由配置、鉴权体系
- 输出了 `mall-swarm拆分后模块说明文档.md`，包含9个服务完整说明（Controller清单、数据表、跨服务调用、路由表、部署架构、文件统计）
- 详细讲解了网关 7 个 Java 文件的代码和作用：MallGatewayApplication、SaTokenConfig、GlobalCorsConfig、IgnoreUrlsConfig、RedisConfig、StpInterfaceImpl、StpMemberUtil

## 微服务模块拆分（黑马商城风格）
- 深入分析了 mall-admin(31个Controller) 和 mall-portal(13个Controller) 的业务边界
- 按 DDD 限界上下文拆分为 9 个独立服务：
  - **user-service** (8081): 后台用户+角色+菜单+资源
  - **product-service** (8082): 商品+品牌+分类+SKU+文件存储
  - **order-service** (8083): 后台订单管理
  - **marketing-service** (8084): 营销(Sms)+内容(Cms)
  - **member-service** (8085): 前台会员+地址+收藏+关注+浏览(MongoDB)
  - **cart-service** (8086): 购物车
  - **portal-service** (8087): 首页聚合(纯Feign编排，无数据库)
  - **trade-service** (8090): 前台下单+支付宝支付+RabbitMQ
  - **search-service** (8093): Elasticsearch全文搜索
- 新增 **mall-api** 模块：集中管理 Feign 接口 + 跨服务 DTO
- 代码风格对齐黑马商城：{功能}-service命名、I前缀Service、impl子包、domain/po-dto-vo-query、@RequiredArgsConstructor注入
- 更新了 parent pom.xml、gateway路由、白名单配置
- user-service 业务代码搬移完成（61个文件），包括：
  - Controller: AdminController（13个接口，@RequiredArgsConstructor注入）
  - Service: IAdminService + AdminServiceImpl（完整搬移原始逻辑：BCrypt认证、Sa-Token登录、角色分配、分页查询、缓存、登录日志）
  - Service: IRoleService + RoleServiceImpl（骨架）
  - Service: UmsAdminCacheService + UmsAdminCacheServiceImpl（Redis缓存）
  - DAO: UmsAdminRoleRelationDao（搬移+改包路径）
  - DTO: AdminLoginDTO、AdminSaveDTO、UpdatePasswordDTO（改名对齐黑马风格）
  - Model: 22个 MBG 实体（UmsAdmin、UmsRole、UmsMenu、UmsResource 等，所有包路径改为 com.mall.user.model）
  - Mapper: 10个 MBG Mapper 接口（改包路径 + import）
  - Mapper XML: 10个 XML 文件（namespace 改为 com.mall.user.mapper）
  - 数据表: 22个 user-service 独享的表

## 全部后端服务搬移完成（总计 583 个文件）
所有 9 个业务服务已完成从 mall-admin/mall-portal 的代码搬移，对齐黑马商城风格：

| 服务 | 文件数 | 来源 | 核心内容 |
|------|--------|------|----------|
| user-service | 61 | mall-admin(Ums) | AdminController(13接口), BCrypt+Sa-Token登录, 角色分配 |
| product-service | 133 | mall-admin(Pms) | 8个Controller, 18张表, OSS/MinIO |
| order-service | 63 | mall-admin(Oms) | 5个Controller, 订单管理 |
| marketing-service | 150 | mall-admin(Sms+Cms) | 12个Controller, 25张表 |
| member-service | 90 | mall-portal(会员) | 6个Controller, MongoDB, Sa-Token JWT |
| cart-service | 14 | mall-portal(购物车) | CartController, 购物车CRUD |
| trade-service | 40 | mall-portal(下单) | 3个Controller, RabbitMQ, 支付宝 |
| portal-service | 18 | mall-portal(首页) | 3个Controller, 纯Feign编排 |
| search-service | 4 | 骨架 | ES搜索 |
| mall-api | 10 | 新增 | 7个FeignClient + DTO + Config |

所有代码改动: @Autowired→@RequiredArgsConstructor, Service加I前缀, Controller去前缀, package→com.mall.{服务名}, domain/po-dto-vo-query分目录

## 第二轮修复：模块完整性补全

用户截图提示有些文件未正确模块化，进行全面排查和修复：

### 修复1: pom.xml 清理 mall-mbg 引用
- 从父 pom.xml 移除 `<mall-mbg.version>` 属性和 dependencyManagement 中的 mall-mbg 依赖
- 从 search-service pom.xml 移除 mall-mbg 依赖（该服务已使用本地模型）
- 原因：mall-mbg 目录已不存在，各服务已拥有自己的 Model/Mapper/XML

### 修复2: mall-auth FeignClient 服务名
- `UmsAdminService.java`: `@FeignClient("mall-admin")` → `@FeignClient("user-service")`
- `UmsMemberService.java`: `@FeignClient("mall-portal")` → `@FeignClient("member-service")`
- 路径 `/admin/login` 和 `/sso/login` 在新服务中完全匹配

### 修复3: 迁移缺失的 DAO XML（共11个）
- **user-service** (+1): UmsAdminRoleRelationDao.xml
- **order-service** (+3): OmsOrderDao.xml, OmsOrderOperateHistoryDao.xml, OmsOrderReturnApplyDao.xml
- **marketing-service** (+6): CmsPrefrenceAreaProductRelationDao.xml, CmsSubjectProductRelationDao.xml, SmsCouponDao.xml, SmsCouponProductCategoryRelationDao.xml, SmsCouponProductRelationDao.xml, SmsFlashPromotionProductRelationDao.xml
- **member-service** (+1): SmsCouponHistoryDao.xml
- 所有 XML 的 namespace/mapper/model 引用统一改为新服务的包路径
- marketing-service 额外添加了 PmsProduct 模型+PmsProductMapper（支持限时购跨表JOIN）
- member-service 额外添加了 SmsCoupon/SmsCouponHistory 等6个模型+3个Mapper（支持优惠券详情JOIN）

### 修复4: user-service 缺失 Controller 补全（+14个文件）
- 新建 4 个 Controller: MenuController(/menu), ResourceController(/resource), ResourceCategoryController(/resourceCategory), MemberLevelController(/memberLevel)
- 新建 4 个 Service 接口: IMenuService, IResourceService, IResourceCategoryService, IMemberLevelService
- 新建 4 个 Service 实现 + UmsMenuNode DTO + UmsMemberLevelMapper
- 全部采用 `@RequiredArgsConstructor` + `private final` 注入

### 修复5: search-service 完整迁移（+15个文件）
- 从 mall-search 将 ES 搜索功能完整迁移到 search-service
- 11 个 Java 源文件: EsProductController(7端点), IEsProductService, EsProductServiceImpl, EsProductDao, EsProductRepository, EsProduct, EsProductAttributeValue, EsProductRelatedInfo, MyBatisConfig, SpringDocConfig
- 4 个资源/配置文件: EsProductDao.xml, pom.xml, application.yaml, application-dev.yaml
- 包路径统一为 `com.mall.search.*`

### 已知待处理项
- 原始模块 mall-admin/mall-portal/mall-search 仍存在（未删除），但已不在父 pom module 列表中
- portal-service 的 Feign Client 注入仍被注释（跨服务调用链路未完全打通）
- mall-auth 中部分 DTO 仍引用旧包路径（如 UmsAdminLoginParam）
- trade-service 和 order-service 存在 OMS 模型重复

## 第三轮修复：清理旧模块 + 修复所有剩余问题

### 旧模块清理
- 删除 mall-admin/、mall-portal/、mall-search/ 三个旧模块目录（不在父 pom 模块列表中）

### CommonResult/CommonPage 导入批量修复（34个文件）
- 所有 `com.mall.common.api.CommonResult` → `com.mym.mall.common.api.CommonResult`
- 所有 `com.mall.common.api.CommonPage` → `com.mym.mall.common.api.CommonPage`  
- 所有 `com.mall.common.constant.AuthConstant` → `com.mym.mall.common.constant.AuthConstant`
- 所有 `com.mall.common.exception.Asserts` → `com.mym.mall.common.exception.Asserts`
- 所有 `com.mall.common.service.RedisService` → `com.mym.mall.common.service.RedisService`
- 覆盖了 6 个服务：user-service, trade-service, marketing-service, order-service, cart-service, mall-api

### portal-service 修复（+11 model + 13 file updates）
- 从 product-service 复制 PmsProduct/PmsBrand/PmsProductCategory 等 8 个 model
- 从 marketing-service 复制 SmsCoupon/SmsHomeAdvertise/CmsSubject 等 3 个 model
- 所有 model 包路径改为 `com.mall.portal.model`
- 更新 13 个 Java 文件的 import 路径

### trade-service OrderServiceImpl 完整修复（+28 文件）
- 复制 12 个 model 类到 trade-service（UmsMember, UmsMemberReceiveAddress, PmsSkuStock, SmsCoupon 等）
- 复制 4 个 Mapper 接口 + 4 个 XML
- 新建 2 个 DAO（PortalOrderDao, PortalOrderItemDao）+ XML
- 新建 4 个 Feign 接口（UmsMemberService, OmsCartItemService, UmsMemberReceiveAddressService, UmsMemberCouponService）
- 新建 2 个 DTO（CartPromotionItem, SmsCouponHistoryDetail）
- 更新 MyBatisConfig 和 TradeApplication 配置

### cart-service 修复（+8 文件）
- 复制 PmsProduct/PmsProductAttribute/PmsSkuStock/UmsMember 模型
- 新建 PortalProductDao + XML
- 新建 2 个 Feign 接口（UmsMemberService, OmsPromotionService）
- 更新 CartProduct/CartServiceImpl/CartController 导入
- 更新 MyBatisConfig 和 CartApplication 配置

### mall-api 补全（+7 文件）
- 新建 BrandClient, SubjectClient, HomeClient, MarketingClient Feign 接口
- 新建 BrandDTO, SubjectDTO
- 更新 ProductClient 新增 search/hot/new/recommend/category/brand 方法

### mall-auth 小修复
- 移除 UmsAdminService.java 中未使用的 RequestParam import

### 最终验证
- ✅ 所有新服务中无 `com.mym.mall.model.*` 引用
- ✅ 所有新服务中无 `com.mym.mall.mapper.*` 引用
- ✅ 所有新服务中无 `com.mym.mall.portal.*` 引用
- ✅ 所有新服务中无 `com.mall.common.*` 引用
- ✅ 14 个模块与父 pom 模块列表完全一致

## 第四轮：编译错误修复 + 数据库分库

### Maven 编译错误逐模块修复（从 26 错误 → BUILD SUCCESS）
- **product-service** (26 errors): 创建 FlagValidator+FlagValidatorClass 到 mall-common；新建 IOssService+OssServiceImpl；复制 4 个 CMS 模型/DAO/Mapper/XML
- **marketing-service** (2 errors): SmsFlashPromotionProductRelationService → IFlashPromotionProductRelationService
- **member-service** (5 errors): 复制 PmsProduct 模型+Mapper+XML；删除重复的 domain/po/MemberProductCollection
- **search-service** (21 errors): 添加 mybatis-spring-boot-starter 依赖；修复 Aggregation 歧义；ES 聚合方法改为 stub（API 版本不兼容）

### 数据库分库（1个单库 → 6个独立库）
| 数据库 | 服务 | 表数 |
|--------|------|------|
| mall_user | user-service | 16 |
| mall_product | product-service | 18 |
| mall_order | order-service + trade-service (共享) | 7 |
| mall_marketing | marketing-service | 25 |
| mall_member | member-service | 9 |
| mall_cart | cart-service | 1 |

- 生成 6 个独立 SQL 文件到 document/sql/
- 更新 7 个服务的 application.yaml 数据源 URL
- 移除 search-service、portal-service 的无用 MySQL 配置
- 删除孤儿 config/admin/ 目录（旧 mall-admin 残留）

## 项目重命名：mall-swarm → mall-cloud

### 修改范围
- **15 个 pom.xml**: 根 pom `<artifactId>mall-swarm</artifactId>` → `<artifactId>mall-cloud</artifactId>`；14 个子模块 `<parent>` 引用同步更新
- **README.md**: 5 处更新（标题、描述、教程链接、环境搭建说明），保留外部 URL（gitee/github）
- **document/微服务拆分完整文档.md**: 14 处更新
- **mall-common/src/main/resources/logback-spring.xml**: 4 处 `"project": "mall-swarm"` → `"project": "mall-cloud"`
- **mall-gateway学习笔记.md**: 2 处更新
- **document/reference/deploy_windows.md**: 3 处更新
- **package-lock.json**: `"name": "mall-swarm"` → `"name": "mall-cloud"`
- **.idea/workspace.xml**: IDE Maven run configuration 名称更新

### 保持不变
- 外部 URL (gitee.com/macrozheng/mall-swarm, github.com/macrozheng/mall-cloud → 实际项目地址未变)
- target/ 构建产物目录
- spring.application.name (各服务独立名称未变，如 user-service, product-service 等)
