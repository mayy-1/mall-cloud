## 2026-07-11

### 完成 MyBatis Example 残留清理
- 清理了 77 个 mapper 接口中残留的 updateByExample* 方法签名
- 为所有非 user-service 的标准实体 mapper 补充了 selectByCondition / deleteByCondition / updateSelectiveByCondition
- search-service EsProductMapperCustom 已合并

### Feign 接口统一迁移到 mall-api
- 删除 cart-service 和 trade-service 中所有本地 Feign 接口（6 个）
- 新建 mall-api Feign: PromotionClient, MemberAddressClient, MemberCouponClient
- 扩展 MemberClient（getCurrentMember/getById/updateIntegration）和 CartClient（listPromotion/delete）
- 新建 mall-api DTO: MemberAddressDTO, CartPromotionItemDTO, CouponHistoryDetailDTO 等 6 个
- 删除冗余本地 model 副本: cart.model.UmsMember, trade.model.UmsMember, trade.model.UmsMemberReceiveAddress 等
- 更新 CartServiceImpl, CartController, OrderServiceImpl, SeckillOrderServiceImpl 使用 mall-api Client + DTO

### 修复 SmsCouponHistoryMapper 缺失方法
- 在 member-service SmsCouponHistoryMapper 中补充 getDetailList / getCouponList 方法声明（原在 Custom mapper 中，已合并到 XML 但 Java 接口遗漏）
- 验证所有 mapper XML 中的方法在 Java 接口中均已声明，无遗漏

### Controller 注解简化
- 42 个 Controller 的 @RequestMapping(method = RequestMethod.XXX) 全部替换为 @GetMapping / @PostMapping
- 43 个 @Controller 改为 @RestController，删除所有方法上的 @ResponseBody
- 清理不再使用的 ResponseBody / Controller / RequestMethod import

