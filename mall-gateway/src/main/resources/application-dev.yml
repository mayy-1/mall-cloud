spring:
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        server-addr: localhost:8848
        file-extension: yaml
  config:
    import:
      - nacos:mall-gateway-dev.yaml?refreshEnabled=true
  data:
    redis:
      lettuce:
        client-resources:
          command-timeout: 60s
