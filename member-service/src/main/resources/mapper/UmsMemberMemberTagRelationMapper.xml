<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.member.mapper.UmsMemberMemberTagRelationMapper">
  <resultMap id="BaseResultMap" type="com.mall.member.model.UmsMemberMemberTagRelation">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="member_id" jdbcType="BIGINT" property="memberId" />
    <result column="tag_id" jdbcType="BIGINT" property="tagId" />
  </resultMap>
  <sql id="Base_Column_List">
    id, member_id, tag_id
  </sql>
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from ums_member_member_tag_relation
    where id = #{id,jdbcType=BIGINT}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
    delete from ums_member_member_tag_relation
    where id = #{id,jdbcType=BIGINT}
  </delete>
  <insert id="insert" parameterType="com.mall.member.model.UmsMemberMemberTagRelation">
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into ums_member_member_tag_relation (member_id, tag_id)
    values (#{memberId,jdbcType=BIGINT}, #{tagId,jdbcType=BIGINT})
  </insert>
  <insert id="insertSelective" parameterType="com.mall.member.model.UmsMemberMemberTagRelation">
    <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Long">
      SELECT LAST_INSERT_ID()
    </selectKey>
    insert into ums_member_member_tag_relation
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="memberId != null">
        member_id,
      </if>
      <if test="tagId != null">
        tag_id,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="memberId != null">
        #{memberId,jdbcType=BIGINT},
      </if>
      <if test="tagId != null">
        #{tagId,jdbcType=BIGINT},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.mall.member.model.UmsMemberMemberTagRelation">
    update ums_member_member_tag_relation
    <set>
      <if test="memberId != null">
        member_id = #{memberId,jdbcType=BIGINT},
      </if>
      <if test="tagId != null">
        tag_id = #{tagId,jdbcType=BIGINT},
      </if>
    </set>
    where id = #{id,jdbcType=BIGINT}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.mall.member.model.UmsMemberMemberTagRelation">
    update ums_member_member_tag_relation
    set member_id = #{memberId,jdbcType=BIGINT},
      tag_id = #{tagId,jdbcType=BIGINT}
    where id = #{id,jdbcType=BIGINT}
  </update>

  <select id="selectByCondition" resultMap="BaseResultMap" parameterType="com.mall.member.model.UmsMemberMemberTagRelation">
    SELECT <include refid="Base_Column_List"/> FROM ums_member_member_tag_relation
    <where>
      <if test="id != null">AND id = #{id}</if>
      <if test="memberId != null">AND member_id = #{memberId}</if>
      <if test="tagId != null">AND tag_id = #{tagId}</if>
    </where>
  </select>

  <delete id="deleteByCondition" parameterType="com.mall.member.model.UmsMemberMemberTagRelation">
    DELETE FROM ums_member_member_tag_relation
    <where>
      <if test="id != null">AND id = #{id}</if>
      <if test="memberId != null">AND member_id = #{memberId}</if>
      <if test="tagId != null">AND tag_id = #{tagId}</if>
    </where>
  </delete>

  <update id="updateSelectiveByCondition">
    update ums_member_member_tag_relation
    <set>
      <if test="record.memberId != null">member_id = #{record.memberId},</if>
      <if test="record.tagId != null">tag_id = #{record.tagId},</if>
    </set>
    <where>
      <if test="condition.id != null">AND id = #{condition.id}</if>
      <if test="condition.memberId != null">AND member_id = #{condition.memberId}</if>
      <if test="condition.tagId != null">AND tag_id = #{condition.tagId}</if>
    </where>
  </update>

</mapper>